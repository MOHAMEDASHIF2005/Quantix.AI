# Leave unset to use the Vite dev proxy (recommended for local dev).
# In production, point this at your deployed FastAPI base URL, e.g.
# VITE_API_BASE_URL=https://api.quantix.yourcompany.com/api/v1
VITE_API_BASE_URL=
